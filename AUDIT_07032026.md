# CODE AUDIT — Pre-Test Confirmation
# Date: 07/03/2026

---

## ✅ STONHARD CRM — index.html
**Repo:** avshi2-maker/work-journal  
**Live:** https://avshi2-maker.github.io/work-journal/  
**Supabase:** vmcipofovheztbjmhwsl  

| Check | Result |
|---|---|
| JS Syntax | ✅ ZERO errors |
| Single file rule | ✅ One index.html |
| Supabase project | ✅ vmcipofovheztbjmhwsl only |
| Lines | 7,129 |

### Tables in use (16):
| Table | Purpose |
|---|---|
| projects | All projects |
| quotes | Quotes header |
| quote_items | Quote line items |
| reports | Smart notes / reports |
| contractors_master | Contractors master list |
| project_contractors_master | Contractors per project |
| contractor_transactions | Payments |
| change_orders | Change orders |
| change_order_items | Change order lines |
| purchase_orders | POs |
| approvals | Approval workflow |
| signatures | Signatures |
| gantt_tasks | Gantt chart |
| photos | Site photos |
| beni_notes | Notes |
| purchase-orders | (duplicate — same as purchase_orders) |

### ⚠️ One Note:
Table name `purchase-orders` (with hyphen) appears alongside `purchase_orders` (with underscore).
These are two separate tables. Confirm in Supabase which one is active — the hyphen version may be a legacy name.

---

## ✅ BENI POCKET — beni_pocket_06032026.html
**Repo:** avshi2-maker/Beni-pocket-Mobile  
**Live:** https://avshi2-maker.github.io/Beni-pocket-Mobile/  
**Supabase:** vmcipofovheztbjmhwsl  

| Check | Result |
|---|---|
| JS Syntax | ✅ ZERO errors |
| Supabase project | ✅ vmcipofovheztbjmhwsl |
| call_log direction field | ✅ included in insert |
| Lines | 1,646 |

### Tables in use (4):
| Table | Purpose |
|---|---|
| reminders | Todos / reminders |
| call_log | Call history |
| quick_replies | WhatsApp quick reply buttons |
| voice_memos | Voice notes |

### ⚠️ One Issue — Hardcoded Fallback in quick_replies:
If Supabase returns an error on load, the app falls back to 6 hardcoded messages:

```
{ label:'אחזור אליך',  message:'שלום, קיבלתי את שיחתך. אחזור אליך בהקדם. תודה — בני' }
{ label:'שלח פרטים',  message:'שלום, אנא שלח לי את הפרטים בוואטסאפ. תודה — בני' }
{ label:'בישיבה',      message:'שלום, אני כרגע בישיבה. אחזור אליך בסיומה. תודה — בני' }
{ label:'שלח תמונה',  message:'שלום, אנא שלח לי תמונה בוואטסאפ. תודה — בני' }
{ label:'מחר בבוקר',  message:'שלום, אטפל בנושא מחר בבוקר. תודה — בני' }
{ label:'לא זמין היום', message:'...' }
```

**These are safety fallbacks only** — they only appear if Supabase completely fails to connect.
In normal operation the app reads from the `quick_replies` table.

### ⚠️ Pending DB task (from previous session):
Run this in Supabase SQL Editor (vmcipofovheztbjmhwsl):
```sql
ALTER TABLE call_log ADD COLUMN IF NOT EXISTS direction text DEFAULT 'incoming';
```
This was noted as pending. The code already sends/reads this column.

---

## Summary

| App | Syntax | Supabase | Ready to test? |
|---|---|---|---|
| Stonhard CRM | ✅ | ✅ | ✅ YES |
| Beni Pocket | ✅ | ✅ | ✅ YES |

Both files are clean. No blocking issues.
The `direction` column ALTER is the only SQL that needs to run before testing Beni call log.

